deployment:
  staging:
    branch: master
    heroku:
      appname: hotcustardpayments
machine:
  services:
    - redis
test:
  override:
    - bundle exec ruby populate_datastore.rb
    - bundle exec rspec
