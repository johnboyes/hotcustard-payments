---
extends: default

rules:
  # 120 chars should be enough, but don't fail if a line is longer
  line-length:
    max: 120
    level: warning
    ignore: |
      /.circleci/config.yml

  indentation:
    ignore: |
      /.circleci/config.yml
